# Steganalysis
This Project is used to do steganalysis, encryption and decryption

# Instructions for setting up the code
1. pull the repo
# If encounter "scripts is disabled on this system", run IDE in admin mode and run the following code in the terminal
Set-ExecutionPolicy RemoteSigned -Scope CurrentUser
2. .venv\Scripts\activate
3. pip install -r requirements.txt